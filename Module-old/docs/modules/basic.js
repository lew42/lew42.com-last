Module(() => "basic");
Module(() => "multiple?");
	// if script.src is the same, add as a sub module?
	// or, wait to define them until after the script is finished, and if there are 2+, bundle them?
		// how would you import these?  then the whole script.src would have... multiple anonymous values?  that's not good...